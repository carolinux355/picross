import 'package:flutter/material.dart';

class PlaySessionPowerupsWidget extends StatelessWidget {
  const PlaySessionPowerupsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [
        Text('PLACEHOLDER POWERUPS GO HERE'),
      ],
    );
  }
}